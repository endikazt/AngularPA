import { TodosTerminadosPipe } from './todos-terminados.pipe';

describe('TodosTerminadosPipe', () => {
  it('create an instance', () => {
    const pipe = new TodosTerminadosPipe();
    expect(pipe).toBeTruthy();
  });
});
