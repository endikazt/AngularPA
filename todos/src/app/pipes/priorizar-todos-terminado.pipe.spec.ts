import { PriorizarTodosTerminadoPipe } from './priorizar-todos-terminado.pipe';

describe('PriorizarTodosTerminadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorizarTodosTerminadoPipe();
    expect(pipe).toBeTruthy();
  });
});
